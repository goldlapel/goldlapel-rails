# goldlapel-rails

> **This package has been consolidated into [`goldlapel`](https://rubygems.org/gems/goldlapel).**

Install with:

```bash
gem install goldlapel
```

Then use `require 'goldlapel/rails'` in your Rails app. See the [Gold Lapel docs](https://goldlapel.com/docs/ruby) for details.
